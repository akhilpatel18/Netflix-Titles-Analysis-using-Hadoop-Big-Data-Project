#!/usr/bin/env python
     import sys
     
     for line in sys.stdin:
         if "show_id" in line:
             continue
         data = line.strip().split(',')
         type_data = data[1]
        print("%s\t1" % type_data)